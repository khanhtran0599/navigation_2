import 'package:flutter/widgets.dart';

class MyButtonWithIcon extends StatelessWidget {
  const MyButtonWithIcon({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}

class MyButtonFill extends StatelessWidget {
  const MyButtonFill({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
class MyButtonOutline extends StatelessWidget {
  const MyButtonOutline({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}

class MyButtonBase extends StatelessWidget {
  const MyButtonBase({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}

class MyButtonGradient extends StatelessWidget {
  const MyButtonGradient({super.key});

  @override

  Widget build(BuildContext context) {
    return Container();
  }
}